import React, {Component, PropTypes} from "react";

export default class WaitingForAnswer extends Component {
    render() {
        return (
            <div>
                <h1>Waiting for other player to answer...</h1>
            </div>
        );
    }
}
